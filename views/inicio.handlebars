<header class="container-md" class="p-3 mb-2 bg-info text-white">
       <img src="/imagens/logo.png" alt="">
        <div class="login">
            <form action="/autenticacao" name="cpLogin" method="POST">
                <input type="email" name="login" placeholder="E-mail..." class="tam_campo">
                <input type="password" name="senha" placeholder="Senha..." class="tam_campo">
                
                <div class='pos_botao_login'>
                <input type="submit" value="Entrar" id="btn_cadas">
                
                
                </div>

            </form>
            
            <a href="cadastro/"><button class="btn btn-primary" id="btn_cadas">Cadastre-se</button></a>
            

        </div>
        {{#each erros_login}}
            <div class="alert alert-danger" role="alert" id="erro_login">{{texto}}</div>
        {{else}}
        {{/each}}

        <div class="clear"></div>

    </header>

    <section id="corpo_inicio">
        <center>
            <h1 class="display-4">Bem-vindo a EderOne</h1>
            <h1 class="display-5">A Rede Social Aberta de Pesquisadores em Ciência de Dados.</h1>
        </center>
        
    </section>